import { NextResponse } from 'next/server';

// Placeholder para manejo de trabajos/jobs
// Este archivo debería implementar la lógica para procesos en segundo plano

export async function GET() {
    return NextResponse.json(
        { message: 'API de jobs no está implementada aún' },
        { status: 501 }
    );
}

export async function POST() {
    return NextResponse.json(
        { message: 'API de jobs no está implementada aún' },
        { status: 501 }
    );
}
